# CODEOWNERS — required reviewers for governance-sensitive paths.
#
# Solo-maintainer demo configuration: every owned path points at @mjhoffmeister.
# In a real org these would be team handles (e.g. @your-org/platform-team) and
# the responder workflow would be owned by a stricter team than app code (see
# DEMO.md, "chicken-and-egg limit"). GitHub silently ignores this entire file
# if any handle is invalid — verify under Settings → Code owners after edits.

# Default owner for everything not otherwise specified.
*                                @mjhoffmeister

# CI / responder pipelines — stricter review required (separate team in prod).
/.github/workflows/              @mjhoffmeister
/.github/workflows/self-heal.yml @mjhoffmeister
/.github/workflows/required-checks.yml @mjhoffmeister

# Issue templates and agent guidance — governance-relevant.
/.github/CI_FAILURE_TEMPLATE.md  @mjhoffmeister
/.github/copilot-instructions.md @mjhoffmeister

# Reset tooling — solo presenters depend on this; protect it.
/scripts/                        @mjhoffmeister

# Demo narration — ok for any maintainer to update.
/DEMO.md                         @mjhoffmeister
